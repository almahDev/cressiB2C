query searchDocuments($acronym: String!, $fields: [String], $where: String) {
  documents(
    acronym: $acronym
    fields: $fields
    where: $where
    account: $account
  ) @context(provider: "vtex.store-graphql") {
    id
  }
}
